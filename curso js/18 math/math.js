// patra pi
Math.PI

// rododear

const numeroD = 2.2;

const redodenoRound=Math.round(numeroD) // va a redondear al numero entero mas cercano seria 2

const redodenoCeil=Math.ceil(numeroD) // siempre va hacia arriba seria 3

const redodenoFloor=Math.floor(numeroD) // siempre va hacia abajo seria 2

const redodenoTrunc=Math.trunc(numeroD) // corta la parte decimal y deja la parte entera

// math.random
// para que sea entre 0 y 100

let numero100 = Math.random()*100

let redondea = Math.round(numero100) // esto lo hacemos para que el numero siempre sea sin decimales.

